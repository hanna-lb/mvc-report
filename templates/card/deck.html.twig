{# templates/card/deck.html.twig #}
{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

<h1>{{ title }}</h1>

{% for card in deck %}
    <div class="card {{ card.colour }}">
        <div class="corner top">
            {{ card.value }}
            <div class="{{ card.colour }}"></div>
        </div>
        <div class="pip {{ card.colour }}"></div>
        <div class="corner bottom">
            {{ card.value }}
            <div class="{{ card.colour }}"></div>
        </div>
    </div>
{% endfor %}

{# <div class="card joker">
    <div class="corner top">Joker</div>
    <div class="pip joker"></div>
    <div class="corner bottom">Joker</div>
</div>

<div class="card back">
    <div class="back">
        <div class="pip unknown"></div>
    </div>
</div> #}

{% endblock %}
